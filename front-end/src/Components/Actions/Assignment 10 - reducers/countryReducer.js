import * as actionTypes from "../Assignment 10 - actions/countryActions";
import axios from "axios";

const initialState = {
  oneCountry: [],
  countries: [],
  error: ""
};

const reducer = (state = intialState, action) => {
  //ToDO
};

export default reducer;
